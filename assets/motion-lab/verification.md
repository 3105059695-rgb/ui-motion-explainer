# 本次导出验证 · 2026-09-23

环境：HyperFrames 0.8.62，GSAP 3.14.2，Windows / Chromium，1920×1080，50 fps。原组件固定在 `heygen-com/hyperframes@1b8f8a43`。

- 合辑实际导出 27.000 秒、1350 帧；X21/X22/X23 各 9.000 秒、450 帧。四个 MP4 均无音轨，通过 ffprobe 参数检查与 FFmpeg 全片解码。
- 检查导出视频的 12 个状态：首帧、位移中间帧、归并、推镜焦点、曲线途中、遮罩展开和尾帧。可见 [关键帧](contact-sheet.jpg)。字体实际加载 3 个本地文件，中文在成片中正确显示。
- `hyperframes check` 对 14 个指定时间点及自动转场采样返回 `ok:true`：运行、布局、对比度错误均为 0。仍有两项警告：生成后的合辑 HTML 较长；X21 5.625 秒的两条引用在归并途中短暂重叠。后者是移动过程，最终两条引用分别停在独立区域，已检查。源工程按三个独立 JS 模块维护。
- 运动模糊实际接入：2.02 秒两组副本各 9 层；阅读停留时组件进入不运动状态。5.12 秒以后通过组级透明度关闭副本，避免子内容变化时出现缓存旧文字。不是仅添加一个无实现的属性。
- 正反向跳转比较 0、2.02、5.7、8.8、12.6、26.98 秒：前五处 RGB 像素完全相同；尾帧 526 个像素（0.0254%）相差 1/255，位于纸张顶部边缘。没有对象或文案状态差异；不宣称逐像素完全相同。

这些检查证明随包视频可用和所述实现已运行；不表示用户已经确认新风格，也不代表真实产品操作或性能验证。
