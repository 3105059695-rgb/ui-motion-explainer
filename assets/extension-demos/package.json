{
  "name": "ui-motion-extension-demos",
  "version": "1.0.0",
  "private": true,
  "description": "Eight original compatible motion extensions; not original reference footage.",
  "scripts": {
    "dev": "hyperframes preview . --no-open",
    "check": "hyperframes check .",
    "render": "hyperframes render . -o preview.mp4 --fps 30 --workers 1 --quality high"
  },
  "devDependencies": {
    "hyperframes": "0.8.38"
  }
}
